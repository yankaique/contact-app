export type LoginType = {
  email: string;
  password: string;
};

export type RegisterType = {
  name: string;
  cpf: string;
  cellphone: string;
  email: string;
  password: string;
  birthday: string;
  confirmPassword: string;
};
